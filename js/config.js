// ── Edit these to customise the app without touching logic ──
const REQUIRED_LOCS  = ['gate','podium','mandir'];   // locations that must be covered each slot
const ALERT_DAYS     = 7;                             // days ahead to show leave alerts
const HANDOVER_WARN  = 15;                            // minutes before slot end to show handover badge
const DEFAULT_PIN    = '1234';                        // fallback PIN if none saved
const APP_VERSION    = 'v5';
